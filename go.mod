// Module path is intentionally short so the repo builds locally with no edits.
// If you publish it and want `go install github.com/USERNAME/haversine-check/...`
// to work, change this line to that path and update the import in cmd/distance.
module haversine-check

go 1.20
